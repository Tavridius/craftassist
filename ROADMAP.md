# 🗺️ ROADMAP — развитие StalZone Helper

От калькулятора крафта к порталу-компаньону по STALZONE.
Разделы: **Аукцион · Крафт · Карта · Гайды · Калькулятор сборок**.
(MVP-архитектура и логика крафта описаны в [PLAN.md](PLAN.md) — здесь только новое.)

---

## Этап 0 — Каркас разделов ✅ НА ПРОДЕ (12 июля 2026)

- Шапка: логотип `SCZ//CRAFT` кликабелен → главная.
- Полоса навигации под шапкой: АУКЦИОН · КРАФТ · КАРТА · ГАЙДЫ · СБОРКИ.
  Нереализованные разделы помечены `DEV` и открывают заглушку с описанием модуля.
- Роутер расширен: `#auction / #map / #guides / #builds` поверх старых
  `#item= / #q= / #profile`.
- Тумблер **«КРАФТЫ: ВСЕ / ДОСТУПНЫЕ»** перенесён из шапки в строку поиска раздела
  крафта. Виден всем: гостю показывается пунктирным и по клику ведёт на вход/профиль
  (фильтр считается по прокачке убежища из профиля EXBO).

---

## Этап 1 — Аукцион: биржа артефактов ✅ НА ПРОДЕ (12 июля 2026)

Дополнительно к плану реализован сервис живых лотов
(`services/artefact_lots.py`): средняя из 5 самых дешёвых лотов корзины,
цикл 30 мин — источник цен калькулятора на первую неделю, пока история
не созрела (`BUILD_PRICE_SOURCE=auto` переключит сам).

**Цель:** отслеживать артефакты, которые сильнее всего РАСТУТ в цене, с разбивкой
по редкости (качеству) и заточке. Пользуемся средними ценами продаж, а не
моментальными лотами — моментальные легко бывают завышены/манипулированы.

### Проверенные факты prod-API (12 июля 2026, живые запросы)

Это фундамент — перепроверять не нужно:

- `GET /{region}/auction/{item}/history?additional=true&limit=200&offset=N` —
  у КАЖДОЙ продажи есть `additional`:
  - `qlt` — качество/редкость **0–5**: Обычный, Необычный, Особый, Редкий,
    Исключительный, Легендарный (имена подтверждены юзером; в базе есть только
    `core.quality.common`);
  - `ptn` — заточка **1–15**; поля НЕТ у незаточенных (нормализовать: нет → 0);
  - `upgrade_bonus` — мелкий индивидуальный «перекач» экземпляра сверх уровня
    (доля, напр. 0.0084 при +13; обычно 0.0) — в корзинах ИГНОРИРУЕМ;
  - `spawn_time` — epoch ms рождения экземпляра (≈ свежесть) — v1 игнорируем.
- Страница ≤ 200 записей; `offset` работает на любую глубину — история хранится
  **с 2018 года**. У старых записей ДРУГОЙ формат (`art_type`, `stats_random`) —
  фильтровать по наличию `qlt`.
- Объёмы: Батарейка `total=630 582`, страница в 200 продаж покрывает ~5–7 часов.
  Цены корзин отличаются на 2 порядка: qlt0+0 ≈ 11 тыс. ₽, qlt5+15 ≈ 3.5 млн ₽ —
  средняя «по всем продажам» для артефактов бессмысленна, корзины обязательны.
- `GET .../lots?additional=true` — те же `qlt`/`ptn` у активных лотов
  (у Батарейки всего 162 лота — влезает в одну страницу).
- Тариф: каждый запрос lots/history = **2 единицы** из 400/мин на приложение.

### Сбор данных — `services/artefact_watch.py` (по образу ingredient_watch)

- Снапшот каждые 6 часов (env `ART_WATCH_HOURS=1,7,13,19` МСК). Для каждого из
  **103 артефактов** (listing, `data` содержит `/artefact/`): history
  `additional=true, limit=200`, листать `offset` пока записи новее прошлого
  снапшота, потолок `ART_WATCH_MAX_PAGES=3`. Для сверхликвидных это выборка
  (не все продажи) — для СРЕДНЕЙ этого достаточно.
- Раскладка каждой продажи в корзину `(item_id, qlt, ptn)`; цена за 1 шт =
  `price/amount`. Храним ВСЕ ptn 0–15 как есть; **UI оперирует уровнями
  +0/+5/+10/+15** (решение юзера), промежуточные заточки в уровневые корзины
  НЕ подмешивать (исказят среднюю) — они просто лежат в своих корзинах.
- Хранение: SQLite `data/market.db`, таблица
  `art_sales_agg(item TEXT, qlt INT, ptn INT, slot TEXT, n INT, sum REAL,
  min REAL, max REAL, PRIMARY KEY(item, qlt, ptn, slot))`,
  slot = ISO-час снапшота. Ретенция 35 дней (чистка при снапшоте).
- Бюджет: 103 × ~1.5 страницы × 4 раза/сутки ≈ 620 запросов/сутки =
  ~1 240 единиц — «шум» (<0.3% суточного бюджета); один снапшот на общем
  троттле (`AUCTION_MIN_INTERVAL`) занимает ~2–4 мин и воркеру цен не мешает.
- **Холодный старт:** данные копятся с включения — полная неделя через 7 дней.
  Опциональный разовый бэкфилл недели пагинацией (`ART_BACKFILL=1`): худший
  случай ~5.8 тыс. запросов ≈ 30 мин полного бюджета — гонять ночью, воркер цен
  на паузу. Без бэкфилла тоже ок, просто ждём неделю.

### Агрегаты и рейтинг роста

- `avg7d(item, qlt, ptn)` = Σsum/Σn по слотам за 7 дней; показываем только при
  Σn ≥ `ART_MIN_SALES=5`, иначе «мало данных».
- Рост: `trend7d = (avg за 0–7 дн − avg за 7–14 дн) / avg за 7–14 дн`;
  `momentum24h = (avg24h − avg7d) / avg7d`. Рейтинг сортируется по trend7d
  (переключатель на 24ч), отсечка по ликвидности (≥ K продаж/период).
- Эндпоинты:
  - `GET /api/artmarket/top?window=7d|24h&qlt=&ptn=` — топ роста и падения;
  - `GET /api/artmarket/{id}` — серии avg по слотам для всех корзин (графики)
    + текущие мин. выкупы по корзинам из lots (on-demand, кэш 300 с).

### Фронт (раздел АУКЦИОН, v1)

- Фильтры: качество 0–5, заточка 0/5/10/15, окно 24ч/7д.
- Две колонки «▸ РАСТУТ В ЦЕНЕ» / «▸ ПАДАЮТ»: иконка, имя + бейдж корзины
  (напр. `Q4 +15`), avg7d, Δ% (зел/красн), спарклайн (реюз `chartSvg`), продаж/нед.
- Карточка артефакта: график цены выбранной корзины, сетка корзин qlt×ptn со
  средними и числом продаж, активные лоты.

## Этап 2 — Калькулятор сборок: РУЧНОЙ + АВТО + ХП ✅ НА ПРОДЕ

Доработки 14 июля 2026 — график продаж в карточке полного аука:
- **Годовая история продаж** (`services/sales_log.py`, таблица `item_sales` в
  market.db): пассивный сбор — каждый ответ /auction/{id}/history (воркер цен,
  вотчер артефактов, открытие карточки) агрегируется по часовым слотам МСК;
  дедуп границей `sale_ts:{item}` (только вперёд). Часы — 35 дней, старше —
  роллап в дневные агрегаты, дни — год (`ITEM_SALES_HOURLY_DAYS/KEEP_DAYS`).
  Открытие карточки добавляет предмет в историю воркера (`request_history`) —
  дальше копится само. `/api/health` → `item_sales`.
- **Эндпоинт** `GET /api/market/item/{id}/sales?days=N|since=&until=` — серия
  {t, n (штук), avg/min/max за 1 шт}; грануляция сама: часы в окне ≤33 дней,
  иначе дни.
- **График в карточке** (/market): SVG — линия средней цены + коридор мин-макс
  + бары объёма; пресеты 24Ч/7Д/30Д/90Д/ГОД, масштабирование выделением мышью
  (рефетч окна), сброс двойным кликом, перекрестие с тултипом. Данные копятся
  с первого открытия карточки — прошлое API отдаёт только последними
  страницами, бэкфилла нет.

Доработки 13 июля 2026, вторая итерация (фидбек юзера по автосборщику):
- **Лимиты заражения — жёсткие**: сборка сверх лимита НЕ выдаётся (раньше — только
  предупреждение). Холод: лимит 1.0 (подтверждён юзером; контейнер его не гасит,
  артефактов-протекторов холода в игре нет — только избегание). Отрицательный net
  любого типа — запас защиты, не вреден (подтверждено юзером).
- **Контрарты** (народное название артефактов, гасящих заражение сильных артов):
  оптимизатор чинит грязное DP-ядро жадной заменой слабейших слотов на контрарты
  (`_swap_repair`, минимум потери ценности на единицу снятого превышения) — сборки
  вида «4 арта + 2 контрарта». Второй эшелон — двойственный подъём λ (цена эмиссии,
  демпфер шага против осцилляций), третий — бан худшего эмиттера. Статический штраф
  CONTAM_PENALTY убран (душил жирных эмиттеров до починки).
- **Живучесть/пулестойкость со знаком в ХП**: вредные (красные) версии статов теперь
  считаются в минус (раньше игнорировались — подбирались арты с −17 живучести).
  Формула на фронте: множитель показывается как (100+живучесть)%, а не «×0.00%».
- **Цвет качества**: имена контейнеров (color добавлен в GameDB/справочник) и брони
  окрашены в селектах (iconSelect понимает labelHtml).

Доработки 13 июля 2026 (коммит a6db225):
- **Редкость**: имя арта окрашено по качеству Q0–Q5; в ручном режиме селектор
  редкости + единое поле «качество %» (85–175%) вместо +/− у статов (выход за
  тир меняет редкость). Статы слота — только чтение.
- **Заражения (минусы артефактов)** — теперь модель и ограничение:
  radiation/thermal/biological/psycho/frost/combustion; эмиссия (красный) —
  константа, защита (зелёный, отриц.) масштабируется. Контейнер гасит
  положительный net своей «Внутренней защитой» (%). Лимиты игрока (рад/темп/био
  1.0, пси 3.0, источник — вики+Steam-гайд) — ограничение оптимизатора (штраф
  эмиттеров + починка баном худшего) и показ в тоталах. Множитель эффективности
  со статов убран (формула игры — сырые суммы).
- **Автоподбор**: карточка арта показывает полные статы + майлстоуны заточки.
- **Новая вкладка «Приведённое ХП»**: броня (заточка из `_variants`, нелинейно)
  + контейнер + бюджет → максимум `(100 + пулестойкость) × живучесть` (формула
  из игры, свип λ). Броня-индекс в GameDB (ленивое чтение уровней),
  `/api/build/hp`. Открытый вопрос: вклад контейнера в пулестойкость (+16 в игре)
  из данных не выводится — не моделируем; свежесть и случайные бонусы заточки — тоже.

## Этап 2 (исходный) — Калькулятор сборок: РУЧНОЙ + АВТО ✅ НА ПРОДЕ (12 июля 2026)

Два режима (решение юзера 13 июля 2026 — «обычный калькулятор тоже делаем»):

- **РУЧНОЙ:** пользователь сам кладёт артефакты в слоты контейнера (как stalcalc,
  но с ценами): выбор артефакта, заточка +0…+15, качество через редактируемое
  значение стата (см. модель качества ниже) → суммарные статы, вес, радиация
  и стоимость сборки по avg7d корзин.
- **АВТО:** пользователь вводит **бюджет**, выбирает **контейнер** и до **3 статов**;
  у каждого — **слайдер важности** (`input type="range"`, 0–100). Система сама
  подбирает оптимальный набор в слоты под бюджет по средненедельным ценам.

### Данные (всё уже есть в stalzone-database, проверено локально)

- **Артефакты (103):** статы в `infoBlocks` типа `range` c ключами
  `stalker.artefact_properties.factor.*` и русскими именами прямо в JSON
  (маппинг руками НЕ нужен). Диапазон `[min; max]` — это окно множителя
  качества: у подавляющего большинства статов `min = 0.85 × max` (или обратное
  1.176 у вредных). Исключения есть (напр. Многогранник: 1.44, 1.85) — парсер
  обязан быть терпимым и не падать на них.
- **Заточка — ФОРМУЛА НЕ НУЖНА (проверено 12 июля 2026):** в базе EXBO лежат
  готовые файлы каждого уровня — `items/artefact/<класс>/_variants/<id>/1..15.json`
  (базовый файл = +0). Проверка на 4l7p: полезные статы растут ЛИНЕЙНО,
  ровно **+2% от базы за уровень** (+15 = ×1.30), вредные (Радиация) НЕ растут.
  Калькулятор читает статы уровня прямо из варианта — никаких U(ptn).
- **Случайные доп-свойства на +5/+10/+15:** каждый 5-й уровень заточки даёт
  экземпляру случайное дополнительное свойство (подтверждено сообществом).
  В статической базе их НЕТ (пер-экземплярные), структура variant-файлов
  идентична обычным. v1 их НЕ моделирует (пометка в UI «+ случайный бонус
  за каждые 5 заточки»); v2 — дать пользователю выбрать свойство вручную.
- **Референс-конкурент:** stalcalc.ru — существующий калькулятор сборок
  артефактов Stalcraft (ручной подбор). Наше отличие: автоподбор под бюджет
  по живым ценам аукциона.
- **Контейнеры (32, категория `containers`):** `Вместимость` (= слоты, напр. 3),
  `Эффективность` (% передачи статов артефактов), `Внутренняя защита`
  (гасит радиацию артефактов внутри), `Вес`, `Ранг`.
- Новый индекс в GameDB при загрузке базы: `artefact_stats[item_id] →
  {stat_key: {min, max, harmful}}`, `containers[id] → {slots, efficiency,
  protection, weight}`, справочник статов `stat_key → русское имя`.

### Модель варианта

Вариант = `(артефакт, qlt 0–5, ptn ∈ {0, 5, 10, 15})` → максимум
103×6×4 = **2472 штук**.

- **Цена** = `avg7d` корзины из этапа 1. Нет цены (мало продаж) → вариант
  недоступен. Цена > бюджета → отсекается сразу (оптимизация юзера — да, так).
- **Статы** = `base × Q(qlt) × U(ptn)`, где base — из диапазона JSON,
  Q/U — таблицы-константы в коде (см. «верификация формул»).

**Модель качества (уточнение юзера, 12 июля 2026):** диапазон `[min; max]` из
JSON = «низ ОБЫЧНОГО … верх ЛЕГЕНДАРНОГО» (qlt 0 … qlt 5); при изучении
артефакта в игре внутри тира выпадает случайный %, но нам это НЕ важно:
- по умолчанию для варианта качества q берём **верх его тира**;
- в UI у артефакта сборки редактируемое значение со стрелками
  (число-спиннер): пользователь может увести значение за пределы тира —
  тогда **качество переключается автоматически** (а с ним и ценовая корзина,
  и пересчёт итогов);
- жёсткие границы редактирования: от низа qlt0 до верха qlt5 (= ровно
  диапазон из JSON, никаких значений вне его).

### Оптимизатор

Классический bounded knapsack с ограничением слотов (повторы артефактов
разрешены — в игре можно класть одинаковые):

- Целевая функция: `Σ по слотам Σ_i w_i × norm_i(stat_i)` − штраф за суммарную
  радиацию сверх внутренней защиты контейнера; `norm_i` — нормировка стата на
  максимум по пулу (статы в разных единицах); статы сборки умножаются на
  эффективность контейнера.
- Алгоритм: DP по (слот ≤ 6) × (бюджет с шагом 0.1%) на numpy —
  ~6 × 1000 × N операций, **миллисекунды–доли секунды**. Считаем на лету,
  прогрева не нужно. Вместе с топ-сборкой отдаём 2–3 альтернативы
  (пересчёт с запретом артефактов из топа).
- `POST /api/build/auto {budget, container, stats: [{key, weight}]}` →
  `{slots: [{item, qlt, ptn, price, stats}], totals: {cost, stats, radiation,
  weight}, warnings: ["корзина Q5+15 малоликвидна — цена ориентировочная", ...]}`.

**Про ресурсы (вопрос юзера «поправь меня»):** расчёт ДЁШЕВ. Дорогая часть —
только данные цен, и она решена снапшотами этапа 1. Никаких лишних запросов к
API калькулятор не делает вообще — читает готовые агрегаты из market.db.

### Формулы статов — ВСЁ ВЕРИФИЦИРОВАНО (13 июля 2026)

**Качество Q(qlt)** — подтверждено скринами Ягодки (9n1w) всех 6 тиров, сошлось
с базой (базовый JSON артефакта = окно ОБЫЧНОГО). Тиры непрерывны (низ тира =
верх предыдущего), шаг **+15% от максимума Q0 за тир**; множитель M от Q0-max:

| qlt | тир | M |
|---|---|---|
| 0 | Обычный | ×0.85 … ×1.00 |
| 1 | Необычный | ×1.00 … ×1.15 |
| 2 | Особый | ×1.15 … ×1.30 |
| 3 | Редкий | ×1.30 … ×1.45 |
| 4 | Исключительный | ×1.45 … ×1.60 |
| 5 | Легендарный | ×1.60 … ×1.75 |

- **Итоговый стат** = `q0max × M × (1 + 0.02×ptn)`; M — редактируемое значение
  (спиннер), тир качества автоопределяется по M; границы M = [0.85; 1.75].
- **Вредные статы** (красные, `formatted.nameColor = C15252`, напр. Радиация)
  НЕ масштабируются ни качеством, ни заточкой (скрины + _variants);
  в расчёте берём верх диапазона (консервативно).
- Полезные статы с отрицательными значениями (Кровотечение: зелёный, «меньше —
  лучше») масштабируются как обычные; сравнение по модулю.
- Упрощения v1 (пометка в UI): свежесть, `upgrade_bonus`, случайные
  доп-свойства +5/+10/+15 не моделируем — в средней цене корзины они и так
  усреднены.
- Открытый вопрос: единицы «Внутренней защиты» контейнера (~89.5) против
  «Радиации» артефактов (~1.25) несопоставимы — v1 показывает оба числа
  раздельно и НЕ мешает радиацию в целевую функцию автоподбора.

### Фронт (раздел СБОРКИ)

- Форма: бюджет (число, ₽) → контейнер (селект с подписью «слоты · эффективность ·
  защита») → до 3 статов (селект из справочника; занятые статы из списка убираются)
  → слайдер 0–100 напротив каждого + текущее значение.
- Кнопка «РАССЧИТАТЬ» → карточки слотов (иконка, имя, бейдж `Q… +…`, цена корзины),
  панель суммарных статов (с учётом эффективности/защиты), итого цена vs бюджет,
  предупреждения ликвидности, share-ссылка `#build=…` (сериализация формы).

### Порядок работ этапа 2

1. Индексы статов артефактов и контейнеров в GameDB (парсер infoBlocks).
2. Таблицы Q/U + верификация по скринам из игры.
3. `POST /api/build/auto` (пул → DP → альтернативы).
4. UI формы и результата.
5. Share-URL и полировка.

## Этап 3 — Гайды

**Что:** статьи по крафту/фарму/снаряге со встроенными живыми ценами и ссылками на
карточки предметов.

- **Контент:** markdown в репо (`content/guides/*.md` + frontmatter: title, slug,
  description). Рендер на бэке в HTML-шаблон.
- **Важно для SEO:** гайды отдавать по нормальным URL `/guides/<slug>` (не хэш!) с
  собственными title/description и ссылками в sitemap. Это требует серверного рендера
  или отдачи готового HTML через nginx.
- **Политика:** не плодить массовые ленды — писать только под подтверждённый спрос
  (3–5 гайдов по реальным частотным запросам, смотреть Я.Метрику/Вебмастер).

## Этап 4 — Интерактивная карта

**v2 ЗАДЕПЛОЕНА (13 июля 2026):** на `/map` — настоящая карта мира из КПК,
цельный спутниковый вид всей Зоны (Leaflet, тайлы).

- **Источник — клиент игры (D:\EXBO):** `pda/global_map/r.X.Y.ol` — 36 регионов
  2048² (сетка 9×4), полный мир 18432×8192. Конвертация `.ol → .dds` (sc-file),
  затем пирамида `frontend/wmap/{z}/{x}/{y}.webp`: zoom 0..6, 256px, 3075 тайлов,
  16 МБ. Генератор — `scripts/gen_map_pyramid.py` (прогонять при обновлении карты
  в игре). Рядом есть `pda/map/` — детальный слой (983 тайла 512², сетка плотнее) —
  кандидат на доп. уровни зума в v3.
- **Движок:** Leaflet 1.9.4, вендорен локально (`frontend/vendor/leaflet`), грузится
  лениво при первом открытии `/map`; `CRS.Simple`, maxBounds, тёмная тема контролов.
- **Бэк:** `GET /api/map/meta` — параметры пирамиды (синхронны генератору).
- **История v1:** сперва сделал 7 бумажных топ-даун карт зон из `decals/maps`
  (с игровыми пометками полей артефактов) — пользователь уточнил, что нужна
  цельная карта из КПК; бумажные удалены (коммит fe6b92b, сами ассеты в истории
  git — при желании можно вернуть слоем «поля артефактов»).

**v3 ЗАДЕПЛОЕНА (13 июля 2026):** двухуровневая карта как в игре.

- **Детальный уровень** `pda/map` (983 региона 512², 1px = 1 блок мира, один общий
  план, облака между локациями — игровые) → пирамида `frontend/dmap` (5416 тайлов,
  22 МБ, `scripts/gen_detail_pyramid.py`). Клик по территории на глобальной →
  `/map/{id}` с fitBounds на bbox территории.
- **Территории** (`map.global.region.<id>.name` из ru.lang): south, sever, wnorth,
  limansk — открыты; dmz (Бораль, карта есть), pripyat, cnpp — закрыты. Позиции на
  глобальной — FFT-сопоставление планов (глобальная ≈ художественный коллаж миров,
  масштаб ≈1.7 блока/px, якоря Бар и Кузня-11; смещения у миров РАЗНЫЕ — юг и север
  раздельно) + визуальная привязка (Любеч, Бораль, Припять, ЧАЭС).
- **Границы/фон:** maxBounds строго по изображению + viscosity, minZoom = вписанный
  вид; белый фон был из-за leaflet.css (лениво грузится ПОСЛЕ styles.css) — !important.

**Дальше (v4):**
- **Не размечены:** Совхоз (красный городок NE на детальной ~ (8..11,-5..-3)?),
  Тихая Заводь, Чернобыль-4, клановые арены (Низина, Роза ветров, Хвойный) — нет
  надёжной позиции на глобальной; уточнить у пользователя по игре.
- **Детальные тайлы сезонные** (сейчас зима со снегом) — обновлять пирамиду при
  смене сезона в игре (прогон двух генераторов).
- **Слой точек:** локации внутри территорий (`map.location.id.*`), артефакты/тайники,
  клик → карточка предмета. Источники: вручную; stalcraftmap.net — интеграция.
- **Позже:** редактор точек для сообщества (авторизация EXBO уже есть) с модерацией.

---

## Рекомендуемый порядок и почему

| # | Раздел | Оценка объёма | Почему в этом месте |
|---|--------|---------------|---------------------|
| 1 | Аукцион (биржа артефактов) | средний | кормит этап 2 ценами avg7d; чем раньше включим снапшоты — тем раньше накопится неделя данных |
| 2 | Сборки (автокалькулятор) | средний | зависит от цен этапа 1; статы уже в базе, оптимизатор дёшев |
| 3 | Гайды | средний (в основном контент) | даёт SEO-трафик остальным разделам; требует не-хэш URL |
| 4 | Карта | большой | сначала решить вопрос ассетов; код — стандартный Leaflet |

**Критичная зависимость:** артефакт-снапшоты этапа 1 надо включить КАК МОЖНО РАНЬШЕ —
без 7 дней накопленных корзинных цен калькулятор сборок считать не по чему
(либо разовый ночной бэкфилл, см. этап 1).

## Сквозные задачи (по мере роста)

- **Роутинг:** ✅ СДЕЛАНО (13 июля 2026) — History API + реальные URL, сервер
  отдаёт index.html с per-route мета (`routers/pages.py`), динамический sitemap,
  лендинг `/vygodno-kraftit`. nginx-fallback не понадобился (всё проксируется на
  приложение, оно и отдаёт SPA-пути). Гайды теперь не заблокированы роутингом.
- **Стиль:** все разделы держать в PDA-терминальной теме (тёмно-зелёный, JetBrains Mono).
- **Метрика:** цели Я.Метрики на переходы по разделам — понять, что реально нужно людям.
- Деплой этапов — только по отдельной команде.
